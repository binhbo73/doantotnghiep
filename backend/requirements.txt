# ============================================================
# DJANGO & REST FRAMEWORK
# ============================================================
Django>=5.2.0
djangorestframework>=3.14.0          # DRF - viết API nhanh
djangorestframework-simplejwt>=5.3.0 # JWT Authentication (access + refresh token)
django-cors-headers>=4.3.0           # CORS cho Frontend
django-extensions>=3.2.0             # TimeStampedModel + custom management commands
django-filter>=23.0                  # Filter nâng cao cho DRF ListView
django-redis>=5.4.0                  # Kết nối Redis
redis>=5.0.1                         # Redis client
celery>=5.3.0                         # Task queue for background summarization
drf-spectacular>=0.27.0              # Swagger UI tự động từ ViewSet

# ============================================================
# DATABASE
# ============================================================
psycopg2-binary>=2.9.9               # Kết nối PostgreSQL

# ============================================================
# SERVER
# ============================================================
gunicorn>=21.2.0                     # Production server (WSGI)
uvicorn[standard]>=0.24.0            # ASGI server + WebSocket runtime deps
websockets>=12.0                     # Explicit WebSocket protocol implementation
wsproto>=1.2.0                       # Alternative WebSocket protocol implementation
python-dotenv>=1.0.0                 # Load biến môi trường từ .env
channels>=4.0.0                      # Django Channels for WebSocket support
channels-redis>=4.0.0                # Redis channel layer for Django Channels

# ============================================================
# VECTOR DATABASE & AI
# ============================================================
qdrant-client==1.17.1                # Kết nối Qdrant Vector DB
ollama>=0.1.0                        # Gọi Ollama LLM (Embedding + Chat)
openai>=1.0.0                        # Gọi llama.cpp qua API chuẩn OpenAI
FlagEmbedding>=1.3.5                 # Native BGE-M3 embeddings in Python

# ============================================================
# DOCUMENT PROCESSING (RAG Pipeline)
# ============================================================
opendataloader-pdf>=2.0.0              # Trích xuất text từ PDF (quality cao hơn docling, #1 benchmarks)
pypdf>=5.0.0                            # Read PDF page counts accurately
flashrank>=0.2.0                     # Re-ranking kết quả RAG
langgraph>=0.0.10                    # Workflow AI (RAG orchestration)
python-docx>=0.8.11                  # Parse DOCX nhanh (5-10x so với docling)
python-pptx>=0.6.21                  # Parse PPTX/PPT
openpyxl>=3.1.0                      # Parse Excel .xlsx (fast)
xlrd>=2.0.1                         # Parse legacy Excel .xls

# ============================================================
# UTILITIES
# ============================================================
email-validator>=2.0.0               # Validate email
python-multipart>=0.0.6              # Xử lý file upload (multipart/form-data)

# ============================================================
# TESTING
# ============================================================
pytest>=8.2.0                        # Test runner
pytest-django>=4.7.0                 # Plugin pytest cho Django
httpx>=0.25.0                        # HTTP client để test API
